# G0 truth, provenance, and reproducibility package v1

Status: **BLOCKED_UPSTREAM / local truth-observation outputs complete / no Gate promotion**

Package ID: `G0_TRUTH_PROVENANCE_V1`
Owner: `A01`
Package base: `docs/agent-fleet-plan-v1-20260829@8bfd73f0cf1b785a29ae212f13212e51fe34231e`
Package base tree: `cfedd363147934f50d1352dae31b7d87d79aa8d9`

## 1. Result

This package replaces the stale hand-written snapshot with an offline,
reproducible observation pipeline. It binds five identities separately:

1. the signed observed `main` tip;
2. the frozen G1 candidate baseline;
3. the Agent control-plane head;
4. the assessed Plan authority commit; and
5. the later live tip of that Plan branch.

It also records exact GitHub compare shapes, open Draft PRs, exact-control-head
workflow conclusions, immutable false promotion guards, and the package
terminal outcome. Branch tips, assessed authority, tested heads, accepted
checks, Gate promotion and release authority are never silently substituted.

## 2. Evidence classification

At the bound control head, one G1-R2 workflow completed successfully, but it is
not G0 evidence. The Agent documentation workflow failed at
`Verify agent development documentation contracts`; skipped workflows are not
green evidence. Consequently:

```text
local_outputs_complete=true
remote_g0_gate_pass=false
g0_exit=false
terminal_outcome=BLOCKED_UPSTREAM
```

## 3. Upstream blocker

`A00-DOC-GATE-ASSERTION-CONFLICT` is owned by A00. The A00 gate currently both
forbids the literal `docs/development/CURRENT_SNAPSHOT_V1.*` in the A00 registry
block and requires an A00 forbidden-surface sentence containing the same
literal. Both assertions cannot pass simultaneously. A01 does not modify the
A00-owned gate; it emits a typed PR request for a narrow repair and exact-head
rerun.

## 4. Reproduction

```bash
bash scripts/ci/check_current_snapshot_v1.sh
bash scripts/ci/check_agent_development_docs_v1.sh
bash scripts/ci/check_canonical_development_plan.sh
bash scripts/ci/check_poco_bft_mainline_truth.sh --pre-cutover
```

Only the first command is locally closed by this package. The second remains
blocked until A00 repairs the contradictory assertion. No later command may be
promoted on the basis of a skipped, stale-head, in-progress, or failed run.

## 5. Invalidation set

Re-observe and regenerate after any default-branch, candidate, control-plane,
assessed-plan, live-plan, workflow-conclusion, machine-truth, or A00 gate
change. Generated bytes must be reviewed as a new evidence epoch.

## 6. Explicit non-claims

```text
production_candidate=false
production_consensus_activation=false
release_ready=false
v1_normative_freeze=false
v1_node_support=false
g0_exit=false
g1_exit=false
```
