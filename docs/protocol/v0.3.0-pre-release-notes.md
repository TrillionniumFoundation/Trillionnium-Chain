# Trillionnium v0.3.0-pre Release Notes

更新日期：2026-02-19  
状态：Pre-release（post-merge）

## 1. 本次变更摘要

### P2：Legacy SubmitResult 默认退役（完成）
- `allow_legacy_submit_result` 默认值：`true -> false`
- 默认链路收敛为：`commit_result + reveal_result`
- 保留参数开关以支持应急回切与兼容测试

对应代码：
- `chain/x/workload/types/params.go`
- `chain/x/workload/keeper/msg_server_pouw_test.go`
- `chain/x/workload/keeper/*`（legacy 相关测试改为显式开启参数）

### 文档与验收口径同步（完成）
- 升级迁移基线已更新为 `allow_legacy_submit_result=false`
- release checklist 已明确：
  - 默认关闭必须拒绝 legacy 提交
  - 仅兼容测试中显式开启 legacy

对应文档：
- `docs/protocol/upgrade-migration-v1.md`
- `docs/protocol/pouw-v0.2-release-checklist.md`
- `docs/protocol/p2-legacy-submit-retirement-pr-notes.md`

### 脚本稳健性增强（完成）
- 修复/增强 P1 负向套件下交易重试与序列处理
- `worker_reconcile_smoke` 增强 JSON 输出解析鲁棒性

对应脚本：
- `scripts/scenario_B_timeout.sh`
- `scripts/scenario_C_challenge.sh`
- `scripts/worker_reconcile_smoke.sh`

## 2. 验收结果

### 测试回归
- `go test ./...`：通过（merge 前已执行）
- P1 负向套件：通过
  - 结果：`total=6 pass=6 fail=0 skip=0`
  - 证据：`data/p1-negative/20260219-151439/summary.txt`

## 3. 风险与回滚

### 已知风险
- 旧客户端若仍调用 legacy `SubmitResult`，默认配置下会失败。

### 回滚策略
- 参数级快速回切：
  - `allow_legacy_submit_result=false -> true`
- 不涉及存储迁移回滚。

## 4. 合并记录

- PR: `#1`（MERGED）
- Merge commit: `ff6dc4a20c139f36e9d9d212edb7a8883fc1ff87`
- 关键提交：
  - `8cf8a02` workload: disable legacy SubmitResult by default
  - `f3e7156` docs: align migration and release checklist for legacy retirement
  - `5765b12` scripts: harden negative suite tx retries for sequence mismatch
  - `b92aa70` scripts: parse tx json output robustly in reconcile smoke

## 5. 下一步建议

1. 合并后固定回归（clean env）：`go test ./...` + `scripts/p0_merge_gate.sh`
2. 输出 `post-merge` 归档目录，沉淀可审计证据
3. 制定 legacy 接口硬退役（删除代码）的前置条件与时间表