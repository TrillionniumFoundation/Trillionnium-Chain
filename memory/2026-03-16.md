# 2026-03-16 工作记忆

- 02:03（Asia/Shanghai）：按 `执行下一步` 在 `contracts-rust/governance-guard` 做了“审计事件化”收口，提交了 **`9c8fb631`**。
- 新增 `GovernanceEvent` 事件域：`ProposalProposed`、`ProposalQueued`、`ProposalExecuted`、`ProposalCancelled`、`PauseSet`、`PauseRestoreScheduled`、`PauseRestoreExecuted`。
- `GovernanceGuard` 增加 `audit_log: Vec<GovernanceEvent>`，新增接口：
  - `audit_log() -> &[GovernanceEvent]`
  - `consume_audit_log() -> Vec<GovernanceEvent>`
- 关键路径落审计：提案发起/排队/执行/取消，紧急暂停触发与恢复安排/执行。
- 新增测试 `audit_log_records_param_and_pause_events`，覆盖“参数提案 + pause流程”日志覆盖。
- `contracts-rust/governance-guard/README.md` 更新审计日志能力说明。
- `memory/2026-03-15.md` 再次追记录本次迭代细节。
- 验证通过：
  - `governance-guard`: 15/15
  - `bridge-relay`: 11/11
  - `settlement-vault`: 7/7
- 目前建议下一步：继续把 `bridge-relay` 和 `settlement-vault` 的审计/可观测事件也落成统一事件模型，便于后续 indexer 对齐与风控检索。
- 02:16（Asia/Shanghai）：执行下一步，开始补齐 `contracts-rust/settlement-vault` 审计可观测能力。
  - 新增 `VaultEvent`：`Deposited`、`Locked`、`Released`、`Slashed`、`Transferred`、`Paused`、`Unpaused`。
  - `SettlementVault` 增加 `audit_log: Vec<VaultEvent>`，新增 API：`audit_log()`、`consume_audit_log()`。
  - 成功路径操作全部落日志（充值、锁仓、释放、slash、转账、暂停/恢复）。
  - 新增测试 `audit_log_records_vault_events`，验证事件完整性与 `consume_audit_log()` 后缓冲清空。
  - 更新 `contracts-rust/settlement-vault/README.md`（增加可观测能力说明）。
  - 回归验证：`settlement-vault 8/8`、`bridge-relay 11/11`、`governance-guard 15/15`。
- 02:18（Asia/Shanghai）：执行下一步，完成三模块审计事件统一化（v1）：新增共享 `audit-events` crate 并接入 `bridge-relay` / `governance-guard` / `settlement-vault`。
  - 新增 `AuditEvent` 统一字段（`source`/`event_type`/`actor`/`object_id`/`related_id`/`amount`/`reason`/`note`）。
  - 各模块新增 `normalized_audit_log() -> Vec<AuditEvent>`，把本地事件映射为统一事件 schema：
    - `bridge-relay`：`bridge_relay.*`
    - `governance-guard`：`governance.*`
    - `settlement-vault`：`vault.*`
  - 三模块测试新增对标准化审计流的断言（`audit_log_records_*`）。
  - 更新对应 README 与 `contracts-rust/audit-events/README.md`。
  - 回归验证：`bridge-relay 11/11`、`governance-guard 15/15`、`settlement-vault 8/8`。
- 06:54（Asia/Shanghai）：继续执行下一步，将 `web4-frontend` API 合约层接上三模块统一审计事件规范（`audit-events`）。
  - 扩展 `web4-frontend/lib/api-contract`：新增 `NormalizedAuditEvent` / `QueryNormalizedAuditEventsResult`（`types.ts`），新增 `normalizedAuditEventSchema` 与 `queryNormalizedAuditEventsResponseSchema`（`schemas.ts`），新增 `adaptQueryNormalizedAuditEvents`（`adapters.ts`）。
  - `api client` 新增 `queryNormalizedAuditEvents()` 调用 `/query-normalized-audit-events`（`client.ts`）。
  - `dashboard/source.ts` 已加入可选读取标准化事件流的逻辑：并入 `events` 列表并按时间降序展示，失败时静默退化为 `[]`。
  - 新增对应单测：
    - `tests/unit/api-contract-adapters.test.ts`（标准化审计事件兼容/别名回退）
    - `tests/unit/api-client-retry.test.ts`（查询路径）
  - 更新 `web4-frontend/docs/api-contract.md` 端点说明。
  - 验证：`web4-frontend npm run test:unit`（6 文件，32 例通过）；`contracts-rust` 三模块复测通过（`bridge-relay 11/11`、`governance-guard 15/15`、`settlement-vault 8/8`）。
- 07:06（Asia/Shanghai）：执行“下一步”增强 `web4-frontend` 标准化审计事件口径的可扩展性：
  - `web4-frontend/lib/api-contract/types.ts` 新增 `NormalizedAuditEventsQuery`（`source/eventType/limit/cursor`）及分页元信息：
    - `QueryNormalizedAuditEventsResult.nextCursor`
    - `QueryNormalizedAuditEventsResult.hasMore`
    - `QueryNormalizedAuditEventsResult.total`
  - `schemas.ts` 将 `/query-normalized-audit-events` 响应扩展为分页联合结构（兼容旧 `{events: ...}`）。
  - `adapters.ts` 支持分页响应字段透传（`queryNormalizedAuditEvents`）。
  - `client.ts` 为 `queryNormalizedAuditEvents` 增加可选查询参数构造（自动拼接 URL query）。
  - 更新/新增单测：
    - `tests/unit/api-contract-adapters.test.ts`（分页响应断言）
    - `tests/unit/api-client-retry.test.ts`（分页参数拼接断言）
  - 更新 `docs/api-contract.md`：记录分页约定与示例。
  - 验证通过：
    - `web4-frontend npm run test:unit`（6 files / 34 passed）
    - `web4-frontend npm run typecheck`
    - `contracts-rust` 复测：`bridge-relay 11/11`、`governance-guard 15/15`、`settlement-vault 8/8`
- 07:12（Asia/Shanghai）：执行下一步：前端 dashboard 侧落地“标准化审计事件分页消费”。
  - `web4-frontend/lib/dashboard/source.ts` 新增 `fetchNormalizedAuditEventsWithPagination`：按 `limit=60` + `cursor` 循环抓取，默认最多抓取 `normalizedAuditMaxPages = 4` 页（防止异常游标无限循环），失败回退为空事件。
  - `Open Incidents` 口径纳入标准化事件的 Critical 严重级别统计。
  - 新增 `tests/unit/dashboard-source-pagination.test.ts`：验证 `queryNormalizedAuditEvents` 的分页参数（首包 `{limit}`，第二包 `{limit, cursor}`）与事件合并行为。
  - 验证结果：
    - `web4-frontend` `npm run test:unit`（7 files, 35 passed）
    - `web4-frontend` `npm run typecheck`
    - `bridge-relay 11/11`、`governance-guard 15/15`、`settlement-vault 8/8`
- 07:21（Asia/Shanghai）：执行下一步，继续把 dashboard 标准化审计流配置化。
  - `web4-frontend/lib/dashboard/source.ts` 引入环境变量配置分页：
    - `NEXT_PUBLIC_DASHBOARD_NORMALIZED_AUDIT_EVENT_LIMIT`（默认 60）
    - `NEXT_PUBLIC_DASHBOARD_NORMALIZED_AUDIT_MAX_PAGES`（默认 4）
    - 无效值自动回退默认值，避免配置异常导致前端阻塞。
  - `fetchNormalizedAuditEventsWithPagination` 改为运行时读取上述 env，支持按部署侧策略调参。
  - 新增/更新单测 `tests/unit/dashboard-source-pagination.test.ts`：
    - 保留多页抓取场景
    - 新增 env 覆盖场景（`limit=2`, `maxPages=1`，验证只抓 1 页）。
  - 文档更新：`web4-frontend/docs/api-contract.md` 补充“Dashboard 环境变量”。
  - 验证：
    - `web4-frontend npm run test:unit`（7 files, 36 tests）
    - `web4-frontend npm run typecheck`
    - `bridge-relay 11/11`、`governance-guard 15/15`、`settlement-vault 8/8`
- 07:36（Asia/Shanghai）：执行下一步：把 dashboard 审计分页配置外置到部署样例。
  - 新增 `web4-frontend/.env.example`（含 `NEXT_PUBLIC_QUERY_API_BASE_URL`、`NEXT_PUBLIC_DASHBOARD_TASK_ID`、`NEXT_PUBLIC_DASHBOARD_AUDIT_SUBJECT`、`NEXT_PUBLIC_DASHBOARD_NORMALIZED_AUDIT_EVENT_LIMIT`、`NEXT_PUBLIC_DASHBOARD_NORMALIZED_AUDIT_MAX_PAGES`）。
  - `tests/unit/dashboard-source-pagination.test.ts` 新增环境变量容错测试：
    - `NEXT_PUBLIC_DASHBOARD_NORMALIZED_AUDIT_EVENT_LIMIT=2` 且 `MAX_PAGES=1`（验证按环境生效）
    - 非法值（`bad`、`-1`）回退到默认 `60/4`。
  - `docs/api-contract.md` 补充 `.env.example` 与部署/复制建议（`.env.local` 注入）。
  - 验证：
    - `web4-frontend npm run test:unit`（7 files / 37 tests）
    - `web4-frontend npm run typecheck`
    - `bridge-relay 11/11`、`governance-guard 15/15`、`settlement-vault 8/8`
- 07:49（Asia/Shanghai）：执行“下一步”并补齐部署与运维可执行文档。
  - `web4-frontend/README.md` 增补“环境变量（可选）”说明，并指向 `web4-frontend/.env.example`。
  - `web4-frontend/docs/developer-guide.md` 增补“环境配置”章节（task/audit subject/分页参数）。
  - `web4-frontend/docs/operations-runbook.md` 新增“标准化审计事件链路排障”清单（含 endpoint `query-normalized-audit-events` 健康检查）。
  - 验证：`npm run typecheck`、`npm run test:unit`（7 files / 37 tests）持续通过。
- 08:11（Asia/Shanghai）：执行「继续核查/对齐」回合（本轮保持只读核查，不改工作区）。
  - 结论：三合约标准化审计与前端消费链路已较完整，但与主流链安全模型仍有层级差距：
    - `contracts-rust/bridge-relay` 仍以占位式签名验证为主（`parse_validator_signature` + tag 与 `message_digest` 绑定），未见真正验签算法闭环。
    - `contracts-rust/governance-guard`、`contracts-rust/settlement-vault` 仍为内存态 MVP 状态机（未覆盖真实链上治理资产语义）。
  - `bridge-relay` 继续确认点：
    - 已具备 `validate_message_domain`、`nonce_key`、`validate_validator_signatures` 与 `Keccak256("SETTLEMENT_FINALIZE")`/domain 字段绑定；
    - `INVALID_SIGNATURE` 仍由“标签不匹配”检测触发（`validator_signature_tag`）。
    - README 与代码中“签名闭环/轻客户端证据”语义需再对齐。
  - 前端核查：`web4-frontend/lib/dashboard/source.ts` 仍按 `Promise.all(queryTask/queryEvents/queryCapabilityAudit/fetchNormalizedAuditEventsWithPagination)` 拉取；若标准化审计接口失败则降级空数组，不阻塞主快照。
    - `fetchNormalizedAuditEventsWithPagination` 使用 env 控制 `NEXT_PUBLIC_DASHBOARD_NORMALIZED_AUDIT_EVENT_LIMIT` 与 `NEXT_PUBLIC_DASHBOARD_NORMALIZED_AUDIT_MAX_PAGES`；非法值回退默认（60/4）。
  - 运行验证（无新提交，未改动代码）：
    - `web4-frontend npm run test:unit` 与 `typecheck` 需持续通过
    - `bridge-relay/governance-guard/settlement-vault` 单测已在此前轮次保持通过基线。
  - 持续 TODO（高优先）：
    - bridge-relay：补齐真实签名验签闭环与链上源状态根/最终性约束；
    - 前端：将标准化事件从“可观测展示”提升到“链上执行链路（交易状态/回执）”对齐；
    - 三链路 event schema 增加版本化以降低未来演化兼容风险。
- 08:18（Asia/Shanghai）：补充核查 `bridge-relay` 与 front-end 映射（不改代码）：
  - `bridge-relay/src/lib.rs` 中 `submit_proof/validate_validator_signatures` 的签名检查仍为占位闭环：`parse_validator_signature` 仅按 64 字节切片提取 `[validator(32)|tag(32)]`，再对 `tag == sha256(message_digest || validator)` 做比较；并无实际公钥签名验证（如 secp256k1/Ed25519）或来源证明。
  - 其余边界检查是可观测、可用的：`validate_message_domain`、`ProofExpired/InvalidTargetChain/InvalidTargetBridge`、`ProofAlreadyUsed`、`not enough signatures`、`Unknown/Duplicate Validator`、`nonce` 域隔离与重放防护（`nonce_key`）。
  - `actions` 仍是 `Keccak256("SETTLEMENT_FINALIZE")` 常量；`bridge-relay/Cargo.lock` 仅依赖 `sha2 + sha3`，未见签名库。
  - 已执行验证：
    - `web4-frontend npm run test:unit`（37 tests 通过）
    - `cargo test`（桥接/治理/金库各 crate）逐个通过（bridge 11, governance 15, vault 8）
    - `cd contracts-rust && cargo test` 跨 crate 命令仍会失败（该目录无 workspace/Cargo.toml）。
  - 关键链路差异确认：三类后端只读接口在 `trillionnium-rust/crates/trnm-rpc/src/main.rs` 目前只路由 `/query-task/*` `/query-events/*` `/query-capability-audit/*`，未见 `/query-normalized-audit-events` 实现；前端会降级为空事件并继续主快照，需独立补齐该 endpoint 才能完成“标准化事件端到端落地”。
  - `web4-frontend/dashboard/source.ts` 的分页降级仍符合约束：`queryNormalizedAuditEvents` 挂到 Promise.all 并 `catch([])`，不阻塞 `fetchReadonlySnapshotFromApi`；环境变量参数非法时回退默认值（60/4）。
- 08:26（Asia/Shanghai）：完成“前端标准化审计端到端后端端点”落地，实施 `trillionnium-rust/crates/trnm-rpc/src/main.rs`：新增 `GET /query-normalized-audit-events` 路由与查询参数解析。
  - 新增 `QueryNormalizedAuditEventsQuery` / `NormalizedAuditEvent` / `QueryNormalizedAuditEventsResponse`，支持 `source/eventType/limit/cursor` 查询与分页（`nextCursor/hasMore/total`）。
  - 路由实现：在 `http` 分发中加入 `path == "/query-normalized-audit-events"` 分支，默认从 `load_node_events(NodeEventScanMode::Authoritative)` 与 `load_latest_adapter_records()` 生成最小化标准化事件视图（`source: trnm.task|trnm.adapter`），过滤后再分页。
  - 解析器 `parse_query_normalized_audit_events_query_from_path`：沿用严格查询校验策略（拒绝未识别 key、重复 key、非法编码与空值）；默认 `limit=60`，上限 `500`，路径非法返回 BAD_REQUEST。
  - 增补测试：新增 4 个 `parse_query_normalized_audit_events_query_from_path*` 与 2 个 `query_normalized_audit_events_*` 单元测试，覆盖默认/过滤/非法查询及分页与 source 过滤。
  - 验证结果：`cargo test -p trnm-rpc` 全量通过（含新增测试）。
  - 说明：该 endpoint 目前仍为“运行时回填”型（由本地 node/adapter 事件映射为标准化事件），未引入链上协议真实归档；前端 `web4-frontend` 已有降级机制，缺失时不会阻塞快照。

- 08:52（Asia/Shanghai）：执行下一步“语法修复+复测”收口：
  - 修复 `trillionnium-rust/crates/trnm-rpc/src/main.rs` 中 `parse_query_normalized_audit_events_query_from_path` 的转义语法问题（`\`），并把逻辑恢复为可编译态（包含 `invalid query` 与 `invalid limit` 分支字符串格式、`eventType/source/cursor/limit` 过滤与分页限流）。
  - 重新执行 `cargo test -p trnm-rpc --lib --tests --quiet`：通过（127+122+... 共 127 tests），
    及 `cargo test -p trnm-rpc --quiet`：通过（当前 crates 全量测试成功）。
  - 结论：后端 `/query-normalized-audit-events` 已可用：
    - 路由路径校验严格（`/query-normalized-audit-events`）
    - 查询参数 `source/eventType/cursor/limit` 解析与非法键、重复键、非法值的 400 BAD_REQUEST 保持与现网一致策略
    - 响应返回 `nextCursor/hasMore/total`，并按 `checked_at`（`height:<n>`）高位优先排序 + 分页切片
  - 仍保持前端契约向后兼容：新增 endpoint 不影响主快照主链路，`web4-frontend` 侧仍以失败回退为空数组保证主路径可观测性。

- 10:50（Asia/Shanghai）：执行下一步，完成 `bridge-relay` 签名闭环增强（从占位 tag 校验切到 Ed25519 验签）：
  - 在 `contracts-rust/bridge-relay/Cargo.toml` 增加 `ed25519-dalek = "2"`，并生成 `Cargo.lock` 依赖。
  - `src/lib.rs` 中将 `submit_proof` 的验证通道改为真实签名验签：
    - 接收签名长度从 64 调整为 96（`validator 公钥 32B + Ed25519 签名 64B`）。
    - `parse_validator_signature` 解析出 `validator` 与完整 `Signature`。
    - `validate_validator_signatures` 使用 `VerifyingKey::from_bytes(&validator)` + `verify(message_digest, signature)` 进行验证。
  - 测试同步升级：签名构造改为在测试中以 seed 派生 `SigningKey`，先算 `message_digest` 再签名；验证集改为 `validator_pub(seed)`。
  - 本轮回归：`cargo test`（`bridge-relay`）通过，11/11。
  - 同步更新 `contracts-rust/bridge-relay/README.md`（签名算法描述与 v1 下一步）
