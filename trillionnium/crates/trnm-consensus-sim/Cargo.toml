[package]
name = "trnm-consensus-sim"
version = "0.1.0"
edition.workspace = true
license.workspace = true
authors.workspace = true
publish = false

[package.metadata.trnm]
lane = "poco-bft-p1-fault-simulator"
protocol = "poco-bft-v0"
network_services = false
wire_conformance = false
genesis_fixture = "signed-qc-independent-block-id-prototype"

[dependencies]
sha2 = { version = "0.10", default-features = false }
trnm-consensus-core = { version = "0.1.0", path = "../trnm-consensus-core" }
trnm-consensus-types = { version = "0.1.0", path = "../trnm-consensus-types" }
