# G1-R2A stack manifest note v1

The mutable package manifest is an evidence pointer, not a release authority.
Its final source/tree fields must be rebound after the implementation branch is
history-normalized. The G1-R1 parent tuple must be independently accepted
before the stack can be rebased or merged.
