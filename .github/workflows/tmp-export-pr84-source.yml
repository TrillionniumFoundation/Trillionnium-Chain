name: Temporary PR84 Source Export

on:
  push:
    branches:
      - codex/tmp-export-pr84-20260907

permissions:
  contents: read

jobs:
  export:
    name: temporary-pr84-source-export
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - name: Checkout exact pushed commit without credentials
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
        with:
          ref: ${{ github.sha }}
          fetch-depth: 1
          persist-credentials: false

      - name: Verify and archive source
        env:
          EXPECTED_SHA: ${{ github.sha }}
        run: |
          set -euo pipefail
          test "$(git rev-parse HEAD)" = "$EXPECTED_SHA"
          git diff --check
          git diff --exit-code -- .
          git diff --cached --exit-code -- .
          tar --exclude=.git -czf "$RUNNER_TEMP/trillionnium-chain-${EXPECTED_SHA}.tar.gz" .
          sha256sum "$RUNNER_TEMP/trillionnium-chain-${EXPECTED_SHA}.tar.gz" > "$RUNNER_TEMP/trillionnium-chain-${EXPECTED_SHA}.tar.gz.sha256"

      - name: Upload exact source snapshot
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: trillionnium-chain-source-${{ github.sha }}
          path: |
            ${{ runner.temp }}/trillionnium-chain-${{ github.sha }}.tar.gz
            ${{ runner.temp }}/trillionnium-chain-${{ github.sha }}.tar.gz.sha256
          if-no-files-found: error
          retention-days: 1
          compression-level: 0
