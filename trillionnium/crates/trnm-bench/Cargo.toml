[package]
name = "trnm-bench"
version = "0.1.0"
edition.workspace = true
license.workspace = true
authors.workspace = true

[dependencies]
clap = { version = "4", features = ["derive"] }
trnm-executor = { version = "0.1.0", path = "../trnm-executor" }
trnm-types = { version = "0.1.0", path = "../trnm-types" }
