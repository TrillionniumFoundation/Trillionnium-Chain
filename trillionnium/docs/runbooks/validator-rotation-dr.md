# Native PoCO Validator Rotation and Disaster Recovery

The authoritative development procedure is
`native-poco-validator-operations.md`.

A validator may resume voting only after authenticated peer formation,
checkpoint verification, state-root convergence, signer-safety verification
and operator approval. Capture all actions in a commit-bound evidence packet.
