name: rust-l1-nightly-health

on:
  push:
    branches:
      - work/plan-v2-registry-closure-20260902
    paths:
      - '.github/workflows/rust-l1-nightly-health.yml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: rust-l1-nightly-health-${{ github.ref }}
  cancel-in-progress: true

jobs:
  superseded-scratch:
    if: false
    runs-on: ubuntu-24.04
    steps:
      - run: 'true'
