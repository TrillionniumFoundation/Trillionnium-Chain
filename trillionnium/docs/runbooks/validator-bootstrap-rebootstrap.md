# Native PoCO Validator Bootstrap and Re-bootstrap

The authoritative development procedure is
`native-poco-validator-operations.md`.

Bootstrap from a clean build, independent keys and authenticated checkpoint.
Re-bootstrap as a non-voting observer and enable voting only after quorum
state-root convergence and signer-safety checks pass.
