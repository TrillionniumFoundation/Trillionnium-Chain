# 2026-03-16 工作记忆

- 02:03（Asia/Shanghai）：按 `执行下一步` 在 `contracts-rust/governance-guard` 做了“审计事件化”收口，提交了 **`9c8fb631`**。
- 新增 `GovernanceEvent` 事件域：`ProposalProposed`、`ProposalQueued`、`ProposalExecuted`、`ProposalCancelled`、`PauseSet`、`PauseRestoreScheduled`、`PauseRestoreExecuted`。
- `GovernanceGuard` 增加 `audit_log: Vec<GovernanceEvent>`，新增接口：
  - `audit_log() -> &[GovernanceEvent]`
  - `consume_audit_log() -> Vec<GovernanceEvent>`
- 关键路径落审计：提案发起/排队/执行/取消，紧急暂停触发与恢复安排/执行。
- 新增测试 `audit_log_records_param_and_pause_events`，覆盖“参数提案 + pause流程”日志覆盖。
- `contracts-rust/governance-guard/README.md` 更新审计日志能力说明。
- `memory/2026-03-15.md` 再次追记录本次迭代细节。
- 验证通过：
  - `governance-guard`: 15/15
  - `bridge-relay`: 11/11
  - `settlement-vault`: 7/7
- 目前建议下一步：继续把 `bridge-relay` 和 `settlement-vault` 的审计/可观测事件也落成统一事件模型，便于后续 indexer 对齐与风控检索。
- 02:16（Asia/Shanghai）：执行下一步，开始补齐 `contracts-rust/settlement-vault` 审计可观测能力。
  - 新增 `VaultEvent`：`Deposited`、`Locked`、`Released`、`Slashed`、`Transferred`、`Paused`、`Unpaused`。
  - `SettlementVault` 增加 `audit_log: Vec<VaultEvent>`，新增 API：`audit_log()`、`consume_audit_log()`。
  - 成功路径操作全部落日志（充值、锁仓、释放、slash、转账、暂停/恢复）。
  - 新增测试 `audit_log_records_vault_events`，验证事件完整性与 `consume_audit_log()` 后缓冲清空。
  - 更新 `contracts-rust/settlement-vault/README.md`（增加可观测能力说明）。
  - 回归验证：`settlement-vault 8/8`、`bridge-relay 11/11`、`governance-guard 15/15`。
- 02:18（Asia/Shanghai）：执行下一步，完成三模块审计事件统一化（v1）：新增共享 `audit-events` crate 并接入 `bridge-relay` / `governance-guard` / `settlement-vault`。
  - 新增 `AuditEvent` 统一字段（`source`/`event_type`/`actor`/`object_id`/`related_id`/`amount`/`reason`/`note`）。
  - 各模块新增 `normalized_audit_log() -> Vec<AuditEvent>`，把本地事件映射为统一事件 schema：
    - `bridge-relay`：`bridge_relay.*`
    - `governance-guard`：`governance.*`
    - `settlement-vault`：`vault.*`
  - 三模块测试新增对标准化审计流的断言（`audit_log_records_*`）。
  - 更新对应 README 与 `contracts-rust/audit-events/README.md`。
  - 回归验证：`bridge-relay 11/11`、`governance-guard 15/15`、`settlement-vault 8/8`。
- 06:54（Asia/Shanghai）：继续执行下一步，将 `web4-frontend` API 合约层接上三模块统一审计事件规范（`audit-events`）。
  - 扩展 `web4-frontend/lib/api-contract`：新增 `NormalizedAuditEvent` / `QueryNormalizedAuditEventsResult`（`types.ts`），新增 `normalizedAuditEventSchema` 与 `queryNormalizedAuditEventsResponseSchema`（`schemas.ts`），新增 `adaptQueryNormalizedAuditEvents`（`adapters.ts`）。
  - `api client` 新增 `queryNormalizedAuditEvents()` 调用 `/query-normalized-audit-events`（`client.ts`）。
  - `dashboard/source.ts` 已加入可选读取标准化事件流的逻辑：并入 `events` 列表并按时间降序展示，失败时静默退化为 `[]`。
  - 新增对应单测：
    - `tests/unit/api-contract-adapters.test.ts`（标准化审计事件兼容/别名回退）
    - `tests/unit/api-client-retry.test.ts`（查询路径）
  - 更新 `web4-frontend/docs/api-contract.md` 端点说明。
  - 验证：`web4-frontend npm run test:unit`（6 文件，32 例通过）；`contracts-rust` 三模块复测通过（`bridge-relay 11/11`、`governance-guard 15/15`、`settlement-vault 8/8`）。
- 07:06（Asia/Shanghai）：执行“下一步”增强 `web4-frontend` 标准化审计事件口径的可扩展性：
  - `web4-frontend/lib/api-contract/types.ts` 新增 `NormalizedAuditEventsQuery`（`source/eventType/limit/cursor`）及分页元信息：
    - `QueryNormalizedAuditEventsResult.nextCursor`
    - `QueryNormalizedAuditEventsResult.hasMore`
    - `QueryNormalizedAuditEventsResult.total`
  - `schemas.ts` 将 `/query-normalized-audit-events` 响应扩展为分页联合结构（兼容旧 `{events: ...}`）。
  - `adapters.ts` 支持分页响应字段透传（`queryNormalizedAuditEvents`）。
  - `client.ts` 为 `queryNormalizedAuditEvents` 增加可选查询参数构造（自动拼接 URL query）。
  - 更新/新增单测：
    - `tests/unit/api-contract-adapters.test.ts`（分页响应断言）
    - `tests/unit/api-client-retry.test.ts`（分页参数拼接断言）
  - 更新 `docs/api-contract.md`：记录分页约定与示例。
  - 验证通过：
    - `web4-frontend npm run test:unit`（6 files / 34 passed）
    - `web4-frontend npm run typecheck`
    - `contracts-rust` 复测：`bridge-relay 11/11`、`governance-guard 15/15`、`settlement-vault 8/8`
- 07:12（Asia/Shanghai）：执行下一步：前端 dashboard 侧落地“标准化审计事件分页消费”。
  - `web4-frontend/lib/dashboard/source.ts` 新增 `fetchNormalizedAuditEventsWithPagination`：按 `limit=60` + `cursor` 循环抓取，默认最多抓取 `normalizedAuditMaxPages = 4` 页（防止异常游标无限循环），失败回退为空事件。
  - `Open Incidents` 口径纳入标准化事件的 Critical 严重级别统计。
  - 新增 `tests/unit/dashboard-source-pagination.test.ts`：验证 `queryNormalizedAuditEvents` 的分页参数（首包 `{limit}`，第二包 `{limit, cursor}`）与事件合并行为。
  - 验证结果：
    - `web4-frontend` `npm run test:unit`（7 files, 35 passed）
    - `web4-frontend` `npm run typecheck`
    - `bridge-relay 11/11`、`governance-guard 15/15`、`settlement-vault 8/8`
- 07:21（Asia/Shanghai）：执行下一步，继续把 dashboard 标准化审计流配置化。
  - `web4-frontend/lib/dashboard/source.ts` 引入环境变量配置分页：
    - `NEXT_PUBLIC_DASHBOARD_NORMALIZED_AUDIT_EVENT_LIMIT`（默认 60）
    - `NEXT_PUBLIC_DASHBOARD_NORMALIZED_AUDIT_MAX_PAGES`（默认 4）
    - 无效值自动回退默认值，避免配置异常导致前端阻塞。
  - `fetchNormalizedAuditEventsWithPagination` 改为运行时读取上述 env，支持按部署侧策略调参。
  - 新增/更新单测 `tests/unit/dashboard-source-pagination.test.ts`：
    - 保留多页抓取场景
    - 新增 env 覆盖场景（`limit=2`, `maxPages=1`，验证只抓 1 页）。
  - 文档更新：`web4-frontend/docs/api-contract.md` 补充“Dashboard 环境变量”。
  - 验证：
    - `web4-frontend npm run test:unit`（7 files, 36 tests）
    - `web4-frontend npm run typecheck`
    - `bridge-relay 11/11`、`governance-guard 15/15`、`settlement-vault 8/8`
- 07:36（Asia/Shanghai）：执行下一步：把 dashboard 审计分页配置外置到部署样例。
  - 新增 `web4-frontend/.env.example`（含 `NEXT_PUBLIC_QUERY_API_BASE_URL`、`NEXT_PUBLIC_DASHBOARD_TASK_ID`、`NEXT_PUBLIC_DASHBOARD_AUDIT_SUBJECT`、`NEXT_PUBLIC_DASHBOARD_NORMALIZED_AUDIT_EVENT_LIMIT`、`NEXT_PUBLIC_DASHBOARD_NORMALIZED_AUDIT_MAX_PAGES`）。
  - `tests/unit/dashboard-source-pagination.test.ts` 新增环境变量容错测试：
    - `NEXT_PUBLIC_DASHBOARD_NORMALIZED_AUDIT_EVENT_LIMIT=2` 且 `MAX_PAGES=1`（验证按环境生效）
    - 非法值（`bad`、`-1`）回退到默认 `60/4`。
  - `docs/api-contract.md` 补充 `.env.example` 与部署/复制建议（`.env.local` 注入）。
  - 验证：
    - `web4-frontend npm run test:unit`（7 files / 37 tests）
    - `web4-frontend npm run typecheck`
    - `bridge-relay 11/11`、`governance-guard 15/15`、`settlement-vault 8/8`
- 07:49（Asia/Shanghai）：执行“下一步”并补齐部署与运维可执行文档。
  - `web4-frontend/README.md` 增补“环境变量（可选）”说明，并指向 `web4-frontend/.env.example`。
  - `web4-frontend/docs/developer-guide.md` 增补“环境配置”章节（task/audit subject/分页参数）。
  - `web4-frontend/docs/operations-runbook.md` 新增“标准化审计事件链路排障”清单（含 endpoint `query-normalized-audit-events` 健康检查）。
  - 验证：`npm run typecheck`、`npm run test:unit`（7 files / 37 tests）持续通过。
- 08:11（Asia/Shanghai）：执行「继续核查/对齐」回合（本轮保持只读核查，不改工作区）。
  - 结论：三合约标准化审计与前端消费链路已较完整，但与主流链安全模型仍有层级差距：
    - `contracts-rust/bridge-relay` 仍以占位式签名验证为主（`parse_validator_signature` + tag 与 `message_digest` 绑定），未见真正验签算法闭环。
    - `contracts-rust/governance-guard`、`contracts-rust/settlement-vault` 仍为内存态 MVP 状态机（未覆盖真实链上治理资产语义）。
  - `bridge-relay` 继续确认点：
    - 已具备 `validate_message_domain`、`nonce_key`、`validate_validator_signatures` 与 `Keccak256("SETTLEMENT_FINALIZE")`/domain 字段绑定；
    - `INVALID_SIGNATURE` 仍由“标签不匹配”检测触发（`validator_signature_tag`）。
    - README 与代码中“签名闭环/轻客户端证据”语义需再对齐。
  - 前端核查：`web4-frontend/lib/dashboard/source.ts` 仍按 `Promise.all(queryTask/queryEvents/queryCapabilityAudit/fetchNormalizedAuditEventsWithPagination)` 拉取；若标准化审计接口失败则降级空数组，不阻塞主快照。
    - `fetchNormalizedAuditEventsWithPagination` 使用 env 控制 `NEXT_PUBLIC_DASHBOARD_NORMALIZED_AUDIT_EVENT_LIMIT` 与 `NEXT_PUBLIC_DASHBOARD_NORMALIZED_AUDIT_MAX_PAGES`；非法值回退默认（60/4）。
  - 运行验证（无新提交，未改动代码）：
    - `web4-frontend npm run test:unit` 与 `typecheck` 需持续通过
    - `bridge-relay/governance-guard/settlement-vault` 单测已在此前轮次保持通过基线。
  - 持续 TODO（高优先）：
    - bridge-relay：补齐真实签名验签闭环与链上源状态根/最终性约束；
    - 前端：将标准化事件从“可观测展示”提升到“链上执行链路（交易状态/回执）”对齐；
    - 三链路 event schema 增加版本化以降低未来演化兼容风险。
- 08:18（Asia/Shanghai）：补充核查 `bridge-relay` 与 front-end 映射（不改代码）：
  - `bridge-relay/src/lib.rs` 中 `submit_proof/validate_validator_signatures` 的签名检查仍为占位闭环：`parse_validator_signature` 仅按 64 字节切片提取 `[validator(32)|tag(32)]`，再对 `tag == sha256(message_digest || validator)` 做比较；并无实际公钥签名验证（如 secp256k1/Ed25519）或来源证明。
  - 其余边界检查是可观测、可用的：`validate_message_domain`、`ProofExpired/InvalidTargetChain/InvalidTargetBridge`、`ProofAlreadyUsed`、`not enough signatures`、`Unknown/Duplicate Validator`、`nonce` 域隔离与重放防护（`nonce_key`）。
  - `actions` 仍是 `Keccak256("SETTLEMENT_FINALIZE")` 常量；`bridge-relay/Cargo.lock` 仅依赖 `sha2 + sha3`，未见签名库。
  - 已执行验证：
    - `web4-frontend npm run test:unit`（37 tests 通过）
    - `cargo test`（桥接/治理/金库各 crate）逐个通过（bridge 11, governance 15, vault 8）
    - `cd contracts-rust && cargo test` 跨 crate 命令仍会失败（该目录无 workspace/Cargo.toml）。
  - 关键链路差异确认：三类后端只读接口在 `trillionnium-rust/crates/trnm-rpc/src/main.rs` 目前只路由 `/query-task/*` `/query-events/*` `/query-capability-audit/*`，未见 `/query-normalized-audit-events` 实现；前端会降级为空事件并继续主快照，需独立补齐该 endpoint 才能完成“标准化事件端到端落地”。
  - `web4-frontend/dashboard/source.ts` 的分页降级仍符合约束：`queryNormalizedAuditEvents` 挂到 Promise.all 并 `catch([])`，不阻塞 `fetchReadonlySnapshotFromApi`；环境变量参数非法时回退默认值（60/4）。
- 08:26（Asia/Shanghai）：完成“前端标准化审计端到端后端端点”落地，实施 `trillionnium-rust/crates/trnm-rpc/src/main.rs`：新增 `GET /query-normalized-audit-events` 路由与查询参数解析。
  - 新增 `QueryNormalizedAuditEventsQuery` / `NormalizedAuditEvent` / `QueryNormalizedAuditEventsResponse`，支持 `source/eventType/limit/cursor` 查询与分页（`nextCursor/hasMore/total`）。
  - 路由实现：在 `http` 分发中加入 `path == "/query-normalized-audit-events"` 分支，默认从 `load_node_events(NodeEventScanMode::Authoritative)` 与 `load_latest_adapter_records()` 生成最小化标准化事件视图（`source: trnm.task|trnm.adapter`），过滤后再分页。
  - 解析器 `parse_query_normalized_audit_events_query_from_path`：沿用严格查询校验策略（拒绝未识别 key、重复 key、非法编码与空值）；默认 `limit=60`，上限 `500`，路径非法返回 BAD_REQUEST。
  - 增补测试：新增 4 个 `parse_query_normalized_audit_events_query_from_path*` 与 2 个 `query_normalized_audit_events_*` 单元测试，覆盖默认/过滤/非法查询及分页与 source 过滤。
  - 验证结果：`cargo test -p trnm-rpc` 全量通过（含新增测试）。
  - 说明：该 endpoint 目前仍为“运行时回填”型（由本地 node/adapter 事件映射为标准化事件），未引入链上协议真实归档；前端 `web4-frontend` 已有降级机制，缺失时不会阻塞快照。

- 08:52（Asia/Shanghai）：执行下一步“语法修复+复测”收口：
  - 修复 `trillionnium-rust/crates/trnm-rpc/src/main.rs` 中 `parse_query_normalized_audit_events_query_from_path` 的转义语法问题（`\`），并把逻辑恢复为可编译态（包含 `invalid query` 与 `invalid limit` 分支字符串格式、`eventType/source/cursor/limit` 过滤与分页限流）。
  - 重新执行 `cargo test -p trnm-rpc --lib --tests --quiet`：通过（127+122+... 共 127 tests），
    及 `cargo test -p trnm-rpc --quiet`：通过（当前 crates 全量测试成功）。
  - 结论：后端 `/query-normalized-audit-events` 已可用：
    - 路由路径校验严格（`/query-normalized-audit-events`）
    - 查询参数 `source/eventType/cursor/limit` 解析与非法键、重复键、非法值的 400 BAD_REQUEST 保持与现网一致策略
    - 响应返回 `nextCursor/hasMore/total`，并按 `checked_at`（`height:<n>`）高位优先排序 + 分页切片
  - 仍保持前端契约向后兼容：新增 endpoint 不影响主快照主链路，`web4-frontend` 侧仍以失败回退为空数组保证主路径可观测性。

- 10:50（Asia/Shanghai）：执行下一步，完成 `bridge-relay` 签名闭环增强（从占位 tag 校验切到 Ed25519 验签）：
  - 在 `contracts-rust/bridge-relay/Cargo.toml` 增加 `ed25519-dalek = "2"`，并生成 `Cargo.lock` 依赖。
  - `src/lib.rs` 中将 `submit_proof` 的验证通道改为真实签名验签：
    - 接收签名长度从 64 调整为 96（`validator 公钥 32B + Ed25519 签名 64B`）。
    - `parse_validator_signature` 解析出 `validator` 与完整 `Signature`。
    - `validate_validator_signatures` 使用 `VerifyingKey::from_bytes(&validator)` + `verify(message_digest, signature)` 进行验证。
  - 测试同步升级：签名构造改为在测试中以 seed 派生 `SigningKey`，先算 `message_digest` 再签名；验证集改为 `validator_pub(seed)`。
  - 本轮回归：`cargo test`（`bridge-relay`）通过，11/11。
  - 同步更新 `contracts-rust/bridge-relay/README.md`（签名算法描述与 v1 下一步）

- 11:09（Asia/Shanghai）：执行下一步，收口“签名闭环后验证与一致性补核”
  - 处理编译时 `as_slice` 生命周期问题后，`bridge-relay` Ed25519 真实验签链路保持绿测（`cargo test --lib --tests`，11 passed）。
  - 复查 `trnm-types` 相关一致性项：
    - 运行 `cargo test -p trnm-types`（`174+` 组测试）全部通过。
    - `i3_capability_issue_renew_revoke_competition.rs` 覆盖线下测试已含 `BridgeSettle` 与 `CapabilityInactive` 优先级场景（revocation/expiry/scope mismatch/未授权均为 `CapabilityInactive` 先返回）。
  - 复核 `trnm-rpc` 端点与标准化审计查询测试：
    - `cargo test -p trnm-rpc query_normalized_audit_events`（含 5 个新测试：defaults/filters/page/source）通过。
    - `cargo test -p trnm-rpc query_capability_audit`（10 tests）通过。
    - `cargo test -p trnm-rpc` / `--lib --tests` 继续通过。
  - 工作区当前 `git status` 干净，最近提交仍为：`3093bc8d`（`fix: close bridge-relay signature verification with Ed25519`）。
  - 下一步建议：继续推进“CapabilityScope/回执绑定”与主链安全语义差距的评估（可先补充文档 + 风险清单，不打断主线）。

- 11:28（Asia/Shanghai）：执行下一步，继续把 bridge-relay 旧占位逻辑清理掉：
  - 移除 `contracts-rust/bridge-relay/src/lib.rs` 中不再使用的 `validator_signature_tag` 辅助函数（签名闭环已改为直接 Ed25519 验签）。
  - 新增测试：
    - `invalid_validator_pubkey_is_rejected_even_if_allowlisted`（允许列表含无效公钥时仍应返回 `InvalidValidatorSignature`）。
  - 回归：
    - `cargo test --lib --tests`（`bridge-relay`）
    - `cargo test -p trnm-rpc query_normalized_audit_events`（5 例通过）
  - 本次保持主流程不变，未改动 `web4-frontend` 前端契约。

- 11:34（Asia/Shanghai）：执行下一步（继续偏差补核）：
  - 复查 `trnm-rpc`/`bridge-relay` 的关键风险链路并完成回归：
    - `cargo test -p trnm-rpc --lib --tests`：OK（包含标准化审计与通用 RPC 路径回归）。
    - `cargo test -p trnm-rpc --test rpc_contract_v1`：8/8 通过（事件/结构形状稳定性）。
    - `cargo test -p trnm-rpc --test query_capability_audit`：10/10 通过。
    - `cargo test -p trnm-rpc --test query_capability_audit` 与 `parse_query_normalized_audit_events...` 相关 3/3 通过。
    - `cargo test -p trnm-types --test i3_capability_issue_renew_revoke_competition`：9/9 通过（含 `BridgeSettle` 与同高并发 `Issue/Renew/Revoke` 场景，`CapabilityInactive` fail-closed 语义稳定）。
    - `cd contracts-rust/bridge-relay && cargo test --lib --tests`：12/12 通过。
  - 结论：目前 `bridge-relay` 已从 tag 匹配升级为 Ed25519 签名验签，`trnm-types` 与 `trnm-rpc` 在 `BridgeSettle + CapabilityInactive` 竞态语义上未观察到新的回归点。
  - 仍待执行的长期差距（未改动）：
    - 标准化审计事件与链上 tx 执行回执（finality/receipt/status）绑定仍未闭环，当前仍主要是状态机/字段对齐验证。
    - `bridge-relay` 未接入真实链上签名密钥生命周期与治理路径（仅示例 Ed25519 验签）。

- 11:54（Asia/Shanghai）：执行下一步（偏差补核：治理链路/签名门限控制）
  - 在 `contracts-rust/bridge-relay/src/lib.rs` 加入治理配置防误配约束：
    - 新增 `BridgeRelayError::InvalidValidatorConfiguration`。
    - `set_min_validator_signatures` 与 `set_validators` 增加最小签名数与集合容量一致性校验：
      - `min_validator_signatures == 0` 禁止
      - `min_validator_signatures > validators.len()` 禁止
      - `validators` 不能为空，且不能 shrink 到低于阈值以下
    - `submit_proof` 在验证前检查上述一致性，避免 `min==0` 触发免费通过。
  - 补充测试：
    - `governance_like_admin_rejects_zero_min_signatures`
    - `governance_like_admin_rejects_empty_validator_set`
    - `governance_like_admin_rejects_validator_set_below_threshold`
    - `governance_like_admin_rejects_configuration_without_validators`
    - `submit_proof_rejects_invalid_validator_signature_configuration`
  - 回归：`contracts-rust/bridge-relay cargo test --lib --tests`：17 passed。
  - `trnm-rpc` 关键回归保持通过（`parse_query_normalized...` / `query_capability_audit`）
  - 说明：这是对“真实 signer 绑定 / governance wiring”链路的一项最小治理硬化，仍建议后续补全真实链上治理/签名密钥生命周期对接。

- 12:20（Asia/Shanghai）：执行下一步（治理接线闭环方向）：
  - 基于前序“签名与配置防误配”收口，新增一版风险闭环评审文档：
    - `docs/reviews/TRNM_REVIEW_BRIDGE_GOVERNANCE_CLOSURE_2026-03-16.md`
  - 文档明确三层残留差距与最小执行清单：
    1) 真实 signer 绑定（proof 与 committee 来源/版本绑定）
    2) Committee auth context 与治理更新接线（治理动作来源、版本化）
    3) 链上回执/finality 字段与 finalize 的二阶段语义
  - 复核与回归：
    - `cargo test -p bridge-relay --lib --tests`（17 passed）
    - `cargo test -p trnm-rpc --lib --tests`（通过）
  - 状态：桥接签名闭环与配置防误配达成；后续仍建议单独推进“治理来源签名上下文 + 回执最终性绑定”最小实现。 

- 12:34（Asia/Shanghai）：执行下一步（治理接线最小实现）：
  - 在 `contracts-rust/bridge-relay/src/lib.rs` 落地 `config_version` 最小版本绑定闭环：
    - `BridgeSettlementMessage` 增加 `config_version: u64`。
    - `submit_proof` 增加 `InvalidConfigVersion` 校验（不匹配直接 fail-closed）。
    - `set_admin` / `set_min_validator_signatures` / `set_validators` 每次配置更新后版本 +1，并写入 `BridgeRelayEvent::ConfigVersionUpdated`。
    - `hash_message` 与 `settlement_id` 纳入 `config_version`，避免配置错配导致重放/历史签名混用。
    - 新增回归测试：
      - `submit_proof_rejects_wrong_config_version`
      - `config_version_bumps_on_admin_rotation`
      - `audit_log_records_admin_and_settlement_flow`（更新 message 版本后校验）
  - 回归：`cargo test --lib --tests`（`contracts-rust/bridge-relay`）19/19；
  - `trnm-rpc --lib --tests` 通过（127）。
  - `README` 同步提及配置版本一致性与新 audit event。
  - 本阶段提交：`206601d9`（`chore: bind bridge-relay proofs to config version`）。

- 12:56（Asia/Shanghai）：执行下一步（外部治理接线最小化）：
  - 在 `bridge-relay` 添加配置版本并发保护的治理入口（最小乐观锁）：
    - 新增 `config_version()` 查询接口。
    - 新增 `set_admin_with_version` / `set_min_validator_signatures_with_version` / `set_validators_with_version`。
    - 这些方法要求调用时携带期望版本，不匹配即返回 `InvalidConfigVersion`（fail-closed），防止并发治理更新/重放错配。
  - 补充回归测试：
    - `config_version_gating_rejects_stale_expected_version`
    - `config_version_gating_accepts_matching_version`
  - 回归验证：
    - `contracts-rust/bridge-relay`：21 passed
    - `trnm-rpc --lib --tests`：通过
  - `README` 同步治理版本并发控制说明。
  - 状态提交：`201fd945`（`docs: add governance protocol for bridge-relay config_version`）

- 13:14（Asia/Shanghai）：执行下一步（治理接线协议文档化）：
  - 新增文档：
    - `docs/reviews/TRNM_REVIEW_BRIDGE_RELAY_CONFIG_VERSION_GOVERNANCE_PROTOCOL_2026-03-16.md`
    - 明确 `config_version` 接线规则：`*_with_version` 乐观锁语义、proof 版本携带要求、失败重试建议。
  - `bridge-relay/README.md` 增补协议入口说明（链接到 review 文档）。
  - 回归状态：
    - `cargo test --lib --tests`（bridge-relay）21/21
    - `cargo test -p trnm-rpc --lib --tests` 正常
  - 本次提交建议：将“配置版本治理接线协议”作为上游治理服务接入前置门槛。

- 13:21（Asia/Shanghai）：执行下一步（治理接线协议收口与 flush）完成：
  - 确认桥接治理链路最新状态：`config_version` 版本化与并发接线已从实现扩展到可执行治理接线协议。
    - 已完成代码层：`set_admin_with_version`/`set_min_validator_signatures_with_version`/`set_validators_with_version`，并保持 `submit_proof`/`finalize` fail-closed。
    - 已完成接线文档：`docs/reviews/TRNM_REVIEW_BRIDGE_RELAY_CONFIG_VERSION_GOVERNANCE_PROTOCOL_2026-03-16.md`。
  - 回归：
    - `contracts-rust/bridge-relay`：`cargo test --lib --tests` 21 passed
    - `trnm-rpc`：`cargo test -p trnm-rpc --lib --tests` 通过
  - 工作区当前已干净（无未提交修改）；新增记录对应提交：
    - `201fd945 docs: add governance protocol for bridge-relay config_version`
    - `50cccc3c docs: record finalized commit hash for config-version protocol docs`
  - 任务状态：本阶段从“版本语义”收口到“上游治理可对接协议化描述”，可进入下一环（链上回执/finality 二阶段约束）。

- 13:32（Asia/Shanghai）：执行下一步（query_capability_audit 失败语义回归补齐）：
  - 在 `trnm-rpc` 中补充查询能力审计接口的 CLI 失败面回归，新增两条单测：
    - `query_capability_audit_rejects_non_numeric_token_id`
    - `query_capability_audit_rejects_missing_token_id_argument`
  - 这两类用例覆盖 token 参数非法/缺失时的 fail-closed 行为。
  - 回归：
    - `cargo test --test query_capability_audit -- --nocapture`（12 passed）
    - `cargo test -p trnm-rpc --lib --tests`（通过）
  - 本阶段建议提交：`af58c6da`（`test: add query-capability-audit missing/invalid token-id regressions`）。

- 13:59（Asia/Shanghai）：执行下一步（tx receipt 与 finalize state 绑定）完成：
  - 在 `contracts-rust/bridge-relay` 落地 `BridgeSettlementMessage` 的执行回执语义最小闭环：
    - 新增 `tx_receipt_status` 字段（`u8`）与成功常量 `TX_RECEIPT_SUCCESS`。
    - `submit_proof`（进而影响 `finalize_settlement`）加入 `validate_message_receipt`：非成功回执直接拒绝。
    - `hash_message` 将 `tx_receipt_status` 纳入签名绑定。
    - 新增测试：
      - `submit_proof_rejects_non_success_tx_receipt`
      - `finalize_settlement_rejects_non_success_tx_receipt`
  - 回归：
    - `contracts-rust/bridge-relay`：`cargo test --lib --tests`（23 passed）
    - `trnm-rpc`：`cargo test -p trnm-rpc --lib --tests`（通过）
  - `bridge-relay/README.md` 的关键 fail-closed 清单新增 `tx receipt` 约束项。
  - 对应提交：`7b316eb0 feat: enforce tx receipt success for bridge settlement proofs`

- 14:22（Asia/Shanghai）：执行下一步（BridgeSettle 执行/回执绑定扩展到 trnm-types）：
  - 在 `trnm-types` 完成最小闭环扩展：`SettlementRecord::apply_status_with_receipt_status`。
    - 新增常量 `SETTLEMENT_TX_RECEIPT_SUCCESS = 1`。
    - 新增错误 `InteropIdentityError::InvalidSettlementReceiptStatus { expected, got }`。
    - `apply_status` 仍兼容旧签名（默认按成功回执处理），新增 helper 允许显式绑定回执状态。
    - 当 `to = SettlementStatus::Finalized` 且状态非 `TX_RECEIPT_SUCCESS` 时，直接 fail-closed。
  - 追加测试：
    - `trnm-types/src/interop_identity.rs::settlement_state_machine_enforces_receipt_success_for_finalization`
    - `trnm-types/tests/x3_settlement_stale_pending_replay.rs::settlement_finalization_rejects_failed_tx_receipt`
  - 回归：
    - `cargo test -p trnm-types --lib --tests` → 174 passed
    - `cargo test -p trnm-types --test x3_settlement_stale_pending_replay` → 2 passed
    - `cargo test --lib --tests`（bridge-relay）→ 23 passed
    - `cargo test -p trnm-rpc --lib --tests` → 通过
  - 对应提交待记：`5f6d512f`（`feat: enforce receipt success for settlement finalization in trnm-types`）

- 15:45（Asia/Shanghai）：执行下一步（桥接回执闭环 smoke 文档化）：
  - 新增最小接线 smoke 脚本：`scripts/check_bridge_settle_receipt_smoke.sh`
    - 覆盖 3 个关键 fail-closed 用例（bridge-relay + trnm-types）。
  - 新增 runbook：`docs/runbooks/bridge_settle_receipt_binding_smoke.md`
    - 说明检查目标、触发失败重试建议与日志路径。
  - 本步执行：`./scripts/check_bridge_settle_receipt_smoke.sh`，结果 PASS。
  - 回归细节（与日志中的 test）：
    - `submit_proof_rejects_non_success_tx_receipt`（pass）
    - `finalize_settlement_rejects_non_success_tx_receipt`（pass）
    - `settlement_state_machine_enforces_receipt_success_for_finalization`（pass）
    - `settlement_finalization_rejects_failed_tx_receipt`（pass）
  - 建议提交：`d4ed746b`（`docs: add bridge-settle receipt binding smoke runbook and script`）

- 15:47（Asia/Shanghai）：修正测试声明与执行脚本噪音：
  - 给 `trnm-types` 中 `settlement_state_machine_enforces_pending_terminal_model` 补上 `#[test]` 标注，避免库测试编译警告。
  - 重新执行 `./scripts/check_bridge_settle_receipt_smoke.sh`（仍 PASS）。


- 15:56（Asia/Shanghai）：本轮收口完成后的 pre-compaction 归档（按 2026-03-16.md）补记：
  - 本批次额外确认：`scripts/check_bridge_settle_receipt_smoke.sh` 与 `docs/runbooks/bridge_settle_receipt_binding_smoke.md` 已稳定存在、可复现；`./scripts/check_bridge_settle_receipt_smoke.sh` 连续执行 PASS。
  - 进一步修正：补齐 `settlement_state_machine_enforces_pending_terminal_model` 的 `#[test]` 标注，避免无用 warning；再次复测仍 PASS。
  - 当前提交序列（本轮）已追加：
    - `d4ed746b`（`docs: add bridge-settle receipt binding smoke runbook and script`）
    - `c3edc5d9`（`docs: fix commit hash in bridge-settle receipt smoke memory note`）
    - `dd9d28c5`（`test: ensure settlement receipt-state test remains annotated`）
  - 进度建议：继续将该回执绑定 smoke 与 CI/running alert 门禁打通；剩余高优先未闭环仍在：
    - finalize/finalization 终态不可回跳与跨路径重放/分域隔离的最小闭环（`bridge-relay finalize_settlement` 与 `action_settlement_finalize` 一致性）
    - `trnm-pouw`/`uw/src/verification/verifiers` 的 `vk_ref` 与 receipt-envelope 边界统一复用扫描

- 15:56（Asia/Shanghai）：继续“下一步”处理 finalize 闭环未完成项，完成 bridge-relay 结算标识域收敛（最小闭环）
  - 修改 `contracts-rust/bridge-relay/src/lib.rs`：`settlement_id` 现在显式包含 `source_bridge_id`、`target_chain_id`、`target_bridge`，与 `hash_message` 的域字段对齐，避免不同域/目标复用同一 settlement id。
  - 新增两个库内测试：
    - `settlement_id_is_scoped_to_domain_fields`
    - `finalize_settlement_is_idempotent_by_settlement_id_even_with_new_nonce`
  - 调整 `contracts-rust/bridge-relay/README.md` fail-closed 测试清单，补充“结算标识域绑定”项（序号更新）。
  - 回归：`cargo test --manifest-path contracts-rust/bridge-relay/Cargo.toml --lib --tests`（25 passed）与 `./scripts/check_bridge_settle_receipt_smoke.sh` 仍 PASS。
  - 说明：此步主要对齐 `action_settlement_finalize` 的分域一致性与终态可回放闭环；后续仍建议继续补齐真实执行链/跨路径 replay 证明层的终态一致性。
