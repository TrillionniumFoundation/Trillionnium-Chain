# P2.3 Stability PR Pack (2026-02-20)

## Recommended PR Title
`rust-l1(p2.3): harden aggressive observability + PR/nightly visibility (non-default strategy)`

## Scope (what this PR includes)

### A) PR-visible regression warning (non-blocking)
- `.github/workflows/trnm-merge-gates.yml`
- Add aggressive regression matrix + `MODE=warn` check on pull requests.
- Emit `::warning` + append full text report into `GITHUB_STEP_SUMMARY`.
- Upload regression artifacts (`aggressive-regression-pr-*.txt`, matrix CSV).

### B) Nightly aggressive summary visibility
- `.github/workflows/rust-l1-nightly-health.yml`
- `trillionnium/scripts/summarize_aggressive_profile.py`
- Build aggressive profile summary and append to `GITHUB_STEP_SUMMARY`.

### C) Executor stage attribution + matrix observability
- `trillionnium/crates/trnm-executor/src/lib.rs`
- `trillionnium/crates/trnm-bench/src/main.rs`
- `trillionnium/scripts/run_bench_regression_matrix.sh`
- Add stage counters (WW/WR/RW checks/hits) and include in bench profile / matrix CSV.

### D) Safe micro-optimization with runtime guard
- `trillionnium/crates/trnm-executor/src/lib.rs`
- Env flag: `TRNM_AGGR_SKIP_EMPTY_STAGE_CHECKS` (default true).
- Skip useless WW/WR or RW checks for empty write/read sets.

### E) Nightly rendered summary richer section
- `trillionnium/scripts/render_nightly_summary.py`
- Add “Aggressive stage hit-rate snapshot” (grouped by workload).

### F) Evidence docs
- `docs/protocol/p2.3-aggressive-scan-window-sweep-20260220.md`
- `docs/protocol/p2.3-aggressive-stage-attribution-20260220.md`
- `docs/protocol/p2.3-aggressive-empty-stage-guard-20260220.md`

---

## Suggested Commit Split

1. **ci(pr): add aggressive regression warning + artifacts (warn-only)**
   - workflow: `trnm-merge-gates.yml`

2. **ci(nightly): publish aggressive profile summary to step summary**
   - workflow: `rust-l1-nightly-health.yml`
   - script: `summarize_aggressive_profile.py`

3. **executor(obs): add aggressive WW/WR/RW stage counters**
   - executor + bench output

4. **bench(obs): extend regression matrix CSV with stage metrics**
   - `run_bench_regression_matrix.sh`

5. **executor(opt): skip empty stage checks (guarded by env)**
   - env: `TRNM_AGGR_SKIP_EMPTY_STAGE_CHECKS`

6. **nightly(summary): include aggressive stage hit-rate snapshot**
   - `render_nightly_summary.py`

7. **docs(p2.3): add sweep + attribution + guard notes**
   - docs/protocol/*.md

---

## PR Checklist (copy to GitHub)

- [ ] `cargo test --workspace` passes
- [ ] `trillionnium/scripts/run_v1_protocol_gates.sh` passes
- [ ] `TXS=2000 trillionnium/scripts/run_bench_regression_matrix.sh` passes
- [ ] `trillionnium/scripts/check_aggressive_regression.sh` passes in hard mode (nightly path)
- [ ] PR run shows warn-only aggressive report in `GITHUB_STEP_SUMMARY`
- [ ] Nightly run shows:
  - [ ] aggressive profiling summary section
  - [ ] aggressive stage hit-rate snapshot section
- [ ] Confirm aggressive strategy remains experimental/non-default
- [ ] Confirm no existing parser/gate break from CSV column extension

---

## Explicit Non-Goals
- Not changing default strategy selection.
- Not claiming aggressive parity with original.
- Not introducing large algorithmic changes in grouping logic.
